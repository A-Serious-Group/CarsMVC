@model List<CarrosMVC.Models.Carroceria>

@{
    ViewData["Title"] = "Carrocerias";
}

<h1>@ViewData["Title"]</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Criar Nova Carroceria</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th>Id</th>
            <th>Nome</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var carroceria in Model)
        {
            <tr>
                <td>@carroceria.Id</td>
                <td>@carroceria.Nome</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@carroceria.Id" class="btn btn-primary">Editar</a>
                    <a asp-action="Delete" asp-route-id="@carroceria.Id" class="btn btn-danger">Excluir</a>
                </td>
            </tr>
        }
    </tbody>
</table>
